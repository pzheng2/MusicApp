<h1><%= @band.name %></h1>
<br>

<ul>
  <% @band.albums.each do |album| %>
    <li><%= album.inspect %>
      <%= link_to "Show Album", album_url(album.id) %>
    </li>
  <% end %>
</ul>

<li><%= link_to "Edit", edit_band_url %></li>

<li><%= link_to "All Bands", bands_url %></li>
